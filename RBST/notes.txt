what is recursion ?
a function that calls itself until it doesn't
( gift box analogy )
def open_gift_box():
   if ball :
      return ball
   open_gift_box()

( a working condition as base condition with return
statement should be there to avoid stack overflow )

---------------------------------------------------
binary search tree cannot have duplicate value
---------------------------------------------------
Deleting a node in a binary search tree :
